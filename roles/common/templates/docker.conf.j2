[Service]
ExecStart=
#ExecStart=/usr/bin/docker daemon --cluster-store=consul://{{ consul_ip}}:8500 --cluster-advertise={{ ansible_default_ipv4.address }}:2375 -H tcp://0.0.0.0:2375 -H unix:///var/run/docker.sock   {{ role_docker }}
ExecStart=/usr/bin/docker daemon  -H  tcp://0.0.0.0:2375 -H unix:///var/run/docker.sock   {{ role_docker }}
